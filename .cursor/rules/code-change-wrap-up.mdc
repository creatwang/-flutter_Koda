---
description: 每次代码变更完成后必须执行收尾（简化 + 验证），不得跳过。
alwaysApply: true
---

# 代码变更收尾（强制）

任何代码编辑、功能实现或 bug 修复结束后、声称完成之前，必须执行收尾。

## 必须做

1. **读取并遵循**项目 skill：`.cursor/skills/code-change-wrap-up/SKILL.md`
2. **代码简化**：对本次触及代码做行为等价的局部精简；无可简化则明确记录「已检查、无可简化」
3. **完成验证**：对本轮变更运行匹配的验证命令（至少 `dart analyze` 相关路径或 `dart analyze lib`），阅读完整输出后再声称结果
4. **禁止**：未跑新鲜验证就说「完成 / 通过 / 已修复」；禁止用「应该没问题」代替证据

## 顺序

```text
实现完成 → 简化 → 验证（新鲜命令输出）→ 才能声称完成 / 进入下一任务
```

纯问答、未改代码时不要求本流程。
